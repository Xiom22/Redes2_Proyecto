export interface Asistencia {
    id?: string,
    carnet?: string,
    nombre?: string,
    evento?: string,
    created_at?: string,
    servidor?: string,
    id_evento?: number,
    foto?:string
}